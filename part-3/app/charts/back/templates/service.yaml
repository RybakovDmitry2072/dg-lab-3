apiVersion: v1
kind: Service
metadata:
  name: {{ include "back.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{- include "back.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "back.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.ports.port }}
      targetPort: {{ .Values.service.ports.targetPort }}
